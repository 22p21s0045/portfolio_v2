/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['localhost','https://portfolio2-database-8qhhg.ondigitalocean.app'],
  }
}

module.exports = nextConfig
